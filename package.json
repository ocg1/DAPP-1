{
  "name": "Ethlend",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "github":"git add -A && git commit -m\"fixes\" && git push origin master"
  },
  "dependencies": {
    "babel-runtime": "^6.23.0",
    "bcrypt": "^0.8.7",
    "moment": "^2.18.1",
    "solc": "^0.4.11",
    "web3": "^0.19.0"
  }
}
